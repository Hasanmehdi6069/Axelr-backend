name = "axelr-backend"
main = "app.py"
compatibility_date = "2024-09-01"
compatibility_flags = ["python_workers"]
python_requirements = "requirements.txt"

[vars]
ADMIN_EMAIL = "shanh1346@gmail.com"
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = "587"
FREE_TIER_TOKEN_LIMIT = "1000000"
GROQ_DAILY_LIMIT = "1000"
OPENROUTER_DAILY_LIMIT = "1000"

# Secrets are NOT stored here – they are set via Dashboard or `wrangler secret put`.
# We'll set them in the Cloudflare Dashboard (no CLI needed).
main = "app.py"
compatibility_flags = ["python_workers"]
compatibility_date = "2024-03-25" # Or your target date
name = "axelr-backend"
main = "app.py" # Ensure this path points exactly to your app script relative to wrangler.toml

compatibility_date = "2024-03-25" 
compatibility_flags = ["python_workers"]
