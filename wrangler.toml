name = "axelr-backend"
main = "app.py"
compatibility_date = "2024-09-01"
compatibility_flags = ["python_workers"]
python_requirements = "requirements.txt"   # ← only once, top-level

[vars]
MONGO_URI = "your-mongo-uri"
GOOGLE_CLIENT_ID = "your-google-client-id"
# ... all other env vars